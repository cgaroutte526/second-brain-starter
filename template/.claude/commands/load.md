You are loading session-start context for the user's Second Brain.

Read the following in this order and produce a brief in-terminal summary. Do NOT write to the vault.

1. `CLAUDE.md` — confirm the constitution is loaded.
2. `areas/_index.md` — current list of areas and projects.
3. The 3 most recent entries in `daily-logs/` (by filename date) — what happened in the last few days.
4. Any project file (`_project.md` or flat `.md` under `areas/`) where the `Active focus:` field is `yes`.

Then write a short briefing with these sections:

**Where I left off:** the most recent shutdown summary's "Tomorrow's seed" if present; otherwise the most recent end-of-day entry.

**Open across active focus:** top open commitments and blockers in active-focus projects, grouped by project. Skip projects without open items.

**Rolling forward:** items that have slipped more than 1 day, with slip count.

**Things to remember:** any "What I learned" notes from the last 3 daily logs.

Keep this under 30 lines total. Surface what is actionable today; do not summarize everything.
